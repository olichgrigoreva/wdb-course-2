import React, { Component } from "react";

export default class AddNewNote extends Component {
  render() {
    return (
      <div className="add-new-note">
        <button className="btn btn-outline-secondary">Add new note</button>
      </div>
    );
  }
}
