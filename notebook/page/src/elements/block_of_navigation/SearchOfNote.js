import React/*, {Component}*/ from 'react';

let SearchOfNote = () => {
  return (
    <input className = "form-control SearchNote"
      placeholder = "Search"
    />
  );
};
export default SearchOfNote;
