import React from 'react';
import './InputContentNote.css';

const InputContentNote = () => {
  return (
    <textarea className = "form-control input-background" placeholder = "содержимое заметки" />
  );
};

export default InputContentNote;
